# Changelog
This document records all notable changes to the **genseqn** project.    
Use the **dev** branch for the latest updates and features.

---

### [2025-12-24]
- Update `config/config.yaml` to align with current pipeline settings
- Update `README.md` to improve documentation clarity and usability
  - Revise and standardize scientific citations
  - Improve overall structure and wording for better readability
  - Update dependencies and software references
  - Clarify workflow steps and processing logic
- Remove unused and obsolete files to clean up the repository
- Add `CHANGELOG.md` to document project history and track changes
- Rename `import_samples.py` to `dataframe.py` for clearer module naming
- Update `dataframe.py` to align with the revised data handling logic
- Update Dockerfile and container image to ensure reproducible environments
- Rename files and directories to `genseqn` for consistent project naming
